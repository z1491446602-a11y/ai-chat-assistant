# 阿里云轻量服务器部署

本文件不再维护独立部署命令，避免与主文档的安全检查、发布 allowlist 和回滚流程发生冲突。

唯一权威流程见根目录 [`README.md`](../../README.md#阿里云轻量服务器部署) 的“阿里云轻量服务器部署”章节。部署、更新与回滚都必须完整执行该章节，不要跳过发布包校验、运行时数据保护或健康检查。

部署前请轮换所有曾在聊天记录中暴露过的服务器凭据和仓库令牌；不要把真实凭据写入文档、命令历史或发布包。
